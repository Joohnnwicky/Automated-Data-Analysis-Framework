---
phase: 01-infrastructure-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified: [src/analysis/analyze_data.py, src/analysis/behavior_analysis.py, src/analysis/statistical_analysis.py, src/analysis/data_overview.py, src/scripts/read_excel.py, src/scripts/read_pptx.py]
autonomous: true
requirements: [INF-02]
user_setup: []

must_haves:
  truths:
    - "All Python scripts run without FileNotFoundError on non-Windows systems"
    - "No hardcoded C:\Users paths exist in src/analysis/*.py"
    - "All scripts use pathlib for path construction"
    - "No global warnings.filterwarnings('ignore') in any script"
  artifacts:
    - path: "src/analysis/analyze_data.py"
      provides: "Statistical analysis with cross-platform paths"
      contains: ["from pathlib import Path", "DATA_DIR"]
      excludes: ["C:\\Users", "warnings.filterwarnings('ignore')"]
    - path: "src/analysis/behavior_analysis.py"
      provides: "Behavioral analysis with cross-platform paths"
      contains: ["from pathlib import Path"]
      excludes: ["C:\\Users"]
    - path: "src/analysis/statistical_analysis.py"
      provides: "Statistical report with cross-platform paths"
      contains: ["from pathlib import Path"]
      excludes: ["C:\\Users"]
    - path: "src/analysis/data_overview.py"
      provides: "Data overview with cross-platform paths"
      contains: ["from pathlib import Path"]
      excludes: ["C:\\Users"]
  key_links:
    - from: "src/analysis/*.py"
      to: "data files"
      via: "DATA_DIR / 'filename'"
      pattern: "Path.*DATA_DIR"
---

<objective>
Move analysis scripts to src/ structure and refactor all hardcoded Windows paths to pathlib for cross-platform compatibility. Remove global warning suppression.

Purpose: Enables scripts to run on macOS/Linux; eliminates platform-specific failures
Output: Analysis scripts moved to src/analysis/ with pathlib paths, warnings suppression removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-PATTERNS.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md
</context>

<interfaces>
From src/config/logging_config.py (created in Plan 03):
```python
def setup_logging(level=logging.INFO, log_file=None):
    """Configure logging for the project."""
```

From tests/conftest.py (created in Plan 01):
```python
src_path = Path(__file__).parent.parent / 'src'
sys.path.insert(0, str(src_path))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move analysis scripts to src/analysis/ and utility scripts to src/scripts/</name>
  <files>src/analysis/analyze_data.py, src/analysis/behavior_analysis.py, src/analysis/statistical_analysis.py, src/analysis/data_overview.py, src/scripts/read_excel.py, src/scripts/read_pptx.py</files>
  <read_first>
    - J:\dataanalyst\analyze_data.py (full file - to be moved)
    - J:\dataanalyst\behavior_analysis.py (full file - to be moved)
    - J:\dataanalyst\statistical_analysis.py (full file - to be moved)
    - J:\dataanalyst\data_overview.py (full file - to be moved)
    - J:\dataanalyst\huashu-data-pro-extracted\huashu-data-pro\scripts\read_excel.py (full file - source for copy)
    - J:\dataanalyst\huashu-data-pro-extracted\huashu-data-pro\scripts\read_pptx.py (full file - source for copy)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 123-139 for structure)
  </read_first>
  <action>Copy analyze_data.py, behavior_analysis.py, statistical_analysis.py, data_overview.py to src/analysis/. Copy read_excel.py, read_pptx.py from huashu-data-pro-extracted/huashu-data-pro/scripts/ to src/scripts/. Keep original files at root for now (will be removed in cleanup phase). Do not modify content yet - just move/copy files to establish structure.</action>
  <verify>
    <automated>ls src/analysis/*.py | wc -l && ls src/scripts/*.py | wc -l</automated>
    <behavior>4 analysis files and 2 script files in src/ directories</behavior>
  </verify>
  <acceptance_criteria>
    - src/analysis/analyze_data.py exists
    - src/analysis/behavior_analysis.py exists
    - src/analysis/statistical_analysis.py exists
    - src/analysis/data_overview.py exists
    - src/scripts/read_excel.py exists
    - src/scripts/read_pptx.py exists
    - src/analysis/ contains 4 .py files
    - src/scripts/ contains 2 .py files
    - Original files preserved at root (not deleted yet)
  </acceptance_criteria>
  <done>6 Python scripts copied to src/ subdirectories</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded paths with pathlib and remove warnings suppression in analysis scripts</name>
  <files>src/analysis/analyze_data.py, src/analysis/behavior_analysis.py, src/analysis/statistical_analysis.py, src/analysis/data_overview.py</files>
  <read_first>
    - J:\dataanalyst\analyze_data.py (lines 1-50 for current patterns)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 280-345 for refactoring patterns)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 149-170 for pathlib pattern)
    - .planning/codebase\CONCERNS.md (lines 12-50 for hardcoded path issues)
  </read_first>
  <action>For each analysis script in src/analysis/: (1) Add import logging and from pathlib import Path at top. (2) Remove or comment out import warnings and warnings.filterwarnings('ignore') lines entirely. (3) Replace hardcoded paths like r'C:\Users\hello\Downloads\...' with DATA_DIR = Path(__file__).parent.parent.parent / 'data' and use DATA_DIR / 'filename.csv'. (4) Replace hardcoded output paths like r'J:\数据分析\...' with OUTPUT_DIR = Path(__file__).parent.parent.parent / 'output' and use OUTPUT_DIR / 'filename.json'. Create output directory with OUTPUT_DIR.mkdir(parents=True, exist_ok=True) before writing. Do NOT replace print() yet - that's Wave 4.</action>
  <verify>
    <automated>grep -c "from pathlib import Path" src/analysis/*.py && grep -c "C:\\\\Users" src/analysis/*.py | grep ":0" | wc -l</automated>
    <behavior>All 4 analysis scripts import pathlib, none contain C:\Users paths</behavior>
  </verify>
  <acceptance_criteria>
    - All src/analysis/*.py files contain "from pathlib import Path"
    - All src/analysis/*.py files define DATA_DIR using Path(__file__)
    - All src/analysis/*.py files define OUTPUT_DIR using Path(__file__)
    - No src/analysis/*.py contains "C:\Users" or "C:\\Users" (grep returns 0)
    - No src/analysis/*.py contains "warnings.filterwarnings('ignore')" (grep returns 0)
    - pd.read_csv uses DATA_DIR / pattern
    - json.dump output uses OUTPUT_DIR / pattern
    - OUTPUT_DIR.mkdir exists in output writing sections
  </acceptance_criteria>
  <done>All hardcoded Windows paths replaced with pathlib, warnings suppression removed from 4 analysis scripts</done>
</task>

<task type="auto">
  <name>Task 3: Add pathlib imports to utility scripts (read_excel.py, read_pptx.py)</name>
  <files>src/scripts/read_excel.py, src/scripts/read_pptx.py</files>
  <read_first>
    - J:\dataanalyst\huashu-data-pro-extracted\huashu-data-pro\scripts\read_excel.py (full file)
    - J:\dataanalyst\huashu-data-pro-extracted\huashu-data-pro\scripts\read_pptx.py (full file)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 397-496 for utility script patterns)
  </read_first>
  <action>For read_excel.py and read_pptx.py in src/scripts/: (1) Verify from pathlib import Path exists (scripts already use argparse with Path). (2) Add import logging near other imports. (3) Add logger = logging.getLogger(__name__) after imports. (4) Replace print(f"读取失败: {e}", file=sys.stderr) with logger.error(f"读取失败: {e}") in exception handlers. Keep print() for CLI stdout output (lines that print to user, not stderr). Do not change other print statements.</action>
  <verify>
    <automated>grep -c "import logging" src/scripts/*.py && grep -c "logger = logging.getLogger" src/scripts/*.py</automated>
    <behavior>Both utility scripts import logging and create logger</behavior>
  </verify>
  <acceptance_criteria>
    - src/scripts/read_excel.py contains "import logging"
    - src/scripts/read_pptx.py contains "import logging"
    - Both scripts contain "logger = logging.getLogger(__name__)"
    - Both scripts have logger.error replacing stderr print in exception handlers
    - CLI output print() statements preserved (not converted to logging)
  </acceptance_criteria>
  <done>Utility scripts updated with logging imports and logger for error handling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → File system | Scripts read data files using Path, no user input for paths yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | Path construction | accept | Paths are hardcoded in code (not user input) - no path traversal risk in Phase 1 |
| T-01-04-02 | Information Disclosure | warnings removal | mitigate | Removing global suppression allows legitimate warnings to surface for debugging |
</threat_model>

<verification>
Wave 2 path refactoring complete when:
- All 4 analysis scripts in src/analysis/
- All scripts import pathlib
- No C:\Users or C:\\Users paths in src/analysis/*.py
- No warnings.filterwarnings('ignore') in src/analysis/*.py
- DATA_DIR and OUTPUT_DIR defined using Path(__file__)

Run: pytest tests/test_infrastructure.py::test_paths_cross_platform -v
Expected: PASS (after this plan completes)
</verification>

<success_criteria>
- [ ] All 4 analysis scripts exist in src/analysis/
- [ ] Both utility scripts exist in src/scripts/
- [ ] All analysis scripts import pathlib.Path
- [ ] No C:\Users hardcoded paths in src/analysis/*.py
- [ ] No warnings.filterwarnings('ignore') in src/analysis/*.py
- [ ] DATA_DIR/OUTPUT_DIR defined with Path(__file__)
- [ ] test_paths_cross_platform passes
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md when done
</output>