---
phase: 01-infrastructure-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-05]
files_modified: []
autonomous: true
requirements: [INF-05]
user_setup: []

must_haves:
  truths:
    - "No __MACOSX directories exist in project"
    - "All pytest tests pass"
    - "Original root scripts removed after successful refactoring"
  artifacts:
    - path: "huashu-data-pro-extracted/__MACOSX/"
      provides: "Cleanup target"
      action: "delete"
    - path: "tests/test_infrastructure.py"
      provides: "Validation of all INF requirements"
      status: "all tests pass"
  key_links:
    - from: "pytest"
      to: "src/analysis/*.py"
      via: "import and assertion"
      pattern: "test_.*pass"
---

<objective>
Clean up __MACOSX artifacts, remove obsolete root-level scripts, and run full test suite to validate all Phase 1 requirements.

Purpose: Final cleanup and validation of infrastructure foundation
Output: Clean project structure, passing tests, obsolete files removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-05-SUMMARY.md
</context>

<interfaces>
From tests/test_infrastructure.py:
```python
def test_requirements_locked()  # INF-01
def test_paths_cross_platform()  # INF-02
def test_logging_levels()  # INF-03
def test_gitignore_patterns()  # INF-04
def test_no_macosx_artifacts()  # INF-05
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete __MACOSX directories</name>
  <files>N/A (directory deletion)</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 277-283 for __MACOSX location)
    - .planning/codebase/CONCERNS.md (lines 159-165 for __MACOSX cleanup recommendation)
  </read_first>
  <action>Delete the entire huashu-data-pro-extracted/__MACOSX/ directory tree. Use rm -rf or PowerShell Remove-Item -Recurse. Verify directory no longer exists. Do NOT delete huashu-data-pro-extracted/huashu-data-pro/ itself (contains SKILL.md and scripts that may be referenced).</action>
  <verify>
    <automated>ls J:\dataanalyst\huashu-data-pro-extracted\__MACOSX 2>&1 | grep -c "No such file"</automated>
    <behavior>__MACOSX directory no longer exists</behavior>
  </verify>
  <acceptance_criteria>
    - huashu-data-pro-extracted/__MACOSX/ directory does not exist
    - rglob('__MACOSX') returns empty list
    - No __MACOSX files in any subdirectory
  </acceptance_criteria>
  <done>__MACOSX artifact directory deleted from project</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all tests pass</name>
  <files>N/A (test execution)</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-VALIDATION.md (lines 39-78 for validation strategy)
    - tests/test_infrastructure.py (created in Plan 01)
  </read_first>
  <action>Run pytest tests/ -v to execute all infrastructure tests. All 5 tests (test_requirements_locked, test_paths_cross_platform, test_logging_levels, test_gitignore_patterns, test_no_macosx_artifacts) must pass. If any test fails, diagnose and fix the underlying issue before marking complete. Capture test output for verification.</action>
  <verify>
    <automated>pytest tests/test_infrastructure.py -v --tb=short</automated>
    <behavior>All 5 tests pass with PASSED status</behavior>
  </verify>
  <acceptance_criteria>
    - pytest tests/ -v runs without errors
    - test_requirements_locked: PASSED
    - test_paths_cross_platform: PASSED
    - test_logging_levels: PASSED
    - test_gitignore_patterns: PASSED
    - test_no_macosx_artifacts: PASSED
    - Test summary shows 5 passed, 0 failed
  </acceptance_criteria>
  <done>All infrastructure tests pass - Phase 1 requirements validated</done>
</task>

<task type="auto">
  <name>Task 3: Remove obsolete root-level analysis scripts</name>
  <files>N/A (file deletion)</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md (scripts moved to src/)
    - J:\dataanalyst\ (directory listing to identify obsolete files)
  </read_first>
  <action>Delete obsolete Python scripts from project root that were moved to src/analysis/: analyze_data.py, behavior_analysis.py, statistical_analysis.py, data_overview.py. Also consider deleting 母婴消费分析.py if it was refactored (verify in src/analysis/ first). These files are now duplicates of the refactored versions in src/. Verify src/analysis/ contains all 4 files before deleting root copies.</action>
  <verify>
    <automated>ls J:\dataanalyst\*.py | grep -E "analyze_data|behavior_analysis|statistical_analysis|data_overview" || echo "Obsolete scripts removed"</automated>
    <behavior>Root-level analysis scripts removed, src/analysis/ contains replacements</behavior>
  </verify>
  <acceptance_criteria>
    - analyze_data.py removed from root (file does not exist)
    - behavior_analysis.py removed from root
    - statistical_analysis.py removed from root
    - data_overview.py removed from root
    - src/analysis/ contains all 4 files (verified before deletion)
    - 母婴消费分析.py handled appropriately (moved or removed if refactored)
  </acceptance_criteria>
  <done>Obsolete root-level scripts removed, only src/ versions remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleanup → File system | Deletes artifact directories and obsolete files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | File deletion | mitigate | Only delete known artifact directories and confirmed obsolete duplicates |
</threat_model>

<verification>
Wave 4 cleanup and validation complete when:
- __MACOSX directory deleted
- All 5 infrastructure tests pass
- Obsolete root scripts removed

Run: pytest tests/ -v
Expected: 5 passed, 0 failed

Final validation:
- pip install -r requirements.txt succeeds
- python -c "from src.config.logging_config import setup_logging" succeeds
- python -c "from src.analysis.analyze_data import *" succeeds (imports work)
</verification>

<success_criteria>
- [ ] __MACOSX directory deleted
- [ ] All 5 pytest tests pass
- [ ] Obsolete root scripts removed
- [ ] requirements.txt installable
- [ ] src/ modules importable
- [ ] test_no_macosx_artifacts passes
- [ ] test_gitignore_patterns passes
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-06-SUMMARY.md when done
Update .planning/STATE.md to mark Phase 1 complete
</output>