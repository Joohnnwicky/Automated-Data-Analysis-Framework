---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified: [src/config/logging_config.py, src/__init__.py, src/config/__init__.py, src/analysis/__init__.py, src/scripts/__init__.py]
autonomous: true
requirements: [INF-03]
user_setup: []

must_haves:
  truths:
    - "setup_logging() function exists and is callable"
    - "src/ directory structure exists with proper package markers"
  artifacts:
    - path: "src/config/logging_config.py"
      provides: "Centralized logging configuration"
      contains: ["setup_logging", "logging.basicConfig"]
      min_lines: 30
    - path: "src/__init__.py"
      provides: "Package marker for src"
    - path: "src/config/__init__.py"
      provides: "Package marker for config"
    - path: "src/analysis/__init__.py"
      provides: "Package marker for analysis"
    - path: "src/scripts/__init__.py"
      provides: "Package marker for scripts"
  key_links:
    - from: "src/config/logging_config.py"
      to: "Application modules"
      via: "from config.logging_config import setup_logging"
      pattern: "setup_logging"
---

<objective>
Create centralized logging configuration module and establish src/ directory structure for modular architecture.

Purpose: Provides reusable logging setup that all scripts will import; establishes package structure for future modules
Output: src/config/logging_config.py, src/ directory with __init__.py markers
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Greenfield - creating new module structure -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ directory structure with package markers</name>
  <files>src/__init__.py, src/config/__init__.py, src/analysis/__init__.py, src/scripts/__init__.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 123-139 for recommended structure)
  </read_first>
  <action>Create directories: src/, src/config/, src/analysis/, src/scripts/. Create empty __init__.py files in each directory to mark as Python packages. Each __init__.py should be minimal (empty or single comment line). Ensure tests/conftest.py sys.path.insert points to src/ (created in Plan 01).</action>
  <verify>
    <automated>test -f src/__init__.py && test -f src/config/__init__.py && test -f src/analysis/__init__.py && test -f src/scripts/__init__.py</automated>
    <behavior>All 4 __init__.py files exist</behavior>
  </verify>
  <acceptance_criteria>
    - src/ directory exists
    - src/config/ directory exists
    - src/analysis/ directory exists
    - src/scripts/ directory exists
    - src/__init__.py exists
    - src/config/__init__.py exists
    - src/analysis/__init__.py exists
    - src/scripts/__init__.py exists
    - All __init__.py files are valid Python (can be imported)
  </acceptance_criteria>
  <done>src/ package structure created with config/, analysis/, scripts/ subpackages</done>
</task>

<task type="auto">
  <name>Task 2: Create src/config/logging_config.py with setup_logging function</name>
  <files>src/config/logging_config.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 185-199 for logging_config pattern)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 56-81 for logging_config.py code)
  </read_first>
  <action>Create src/config/logging_config.py. Import logging, sys, pathlib.Path. Define setup_logging(level=logging.INFO, log_file=None) function. Create StreamHandler(sys.stdout) as default handler. If log_file provided, create parent directories with Path(log_file).parent.mkdir(parents=True, exist_ok=True) and add FileHandler. Call logging.basicConfig with format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', datefmt='%Y-%m-%d %H:%M:%S', handlers list, force=True. Do not configure propagate=False - let child loggers propagate to root.</action>
  <verify>
    <automated>grep -c "def setup_logging" src/config/logging_config.py && grep -c "logging.basicConfig" src/config/logging_config.py</automated>
    <behavior>logging_config.py defines setup_logging with basicConfig</behavior>
  </verify>
  <acceptance_criteria>
    - src/config/logging_config.py exists
    - File imports logging, sys, pathlib.Path
    - File defines function setup_logging(level, log_file)
    - Function creates StreamHandler(sys.stdout)
    - Function handles optional log_file with FileHandler
    - Function calls logging.basicConfig with format string
    - format contains %(asctime)s, %(name)s, %(levelname)s, %(message)s
    - Python can import module: python -c "from src.config.logging_config import setup_logging"
  </acceptance_criteria>
  <done>logging_config.py created with setup_logging function for centralized logging configuration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logging_config → Application | Provides logging setup, no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| None | — | — | — | No security-sensitive operations in this task |
</threat_model>

<verification>
Wave 1 logging infrastructure complete when:
- src/ directory structure exists with all __init__.py
- src/config/logging_config.py exists with setup_logging
- Module can be imported: python -c "from src.config.logging_config import setup_logging"

Test verification pending Wave 3-4 when scripts are refactored.
</verification>

<success_criteria>
- [ ] src/ directory exists
- [ ] src/config/, src/analysis/, src/scripts/ directories exist
- [ ] All 4 __init__.py files exist
- [ ] src/config/logging_config.py exists
- [ ] setup_logging function defined
- [ ] Module importable: python -c succeeds
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md when done
</output>