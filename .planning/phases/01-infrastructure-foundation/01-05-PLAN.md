---
phase: 01-infrastructure-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-04]
files_modified: [src/analysis/analyze_data.py, src/analysis/behavior_analysis.py, src/analysis/statistical_analysis.py, src/analysis/data_overview.py]
autonomous: true
requirements: [INF-03]
user_setup: []

must_haves:
  truths:
    - "All analysis scripts use logging instead of print for diagnostic output"
    - "User can set log level via setup_logging(level=...)"
    - "Log messages include timestamp, module name, and level"
  artifacts:
    - path: "src/analysis/analyze_data.py"
      provides: "Analysis with structured logging"
      contains: ["logger = logging.getLogger(__name__)", "logger.info"]
      excludes: ["print("]
    - path: "src/analysis/behavior_analysis.py"
      provides: "Behavior analysis with structured logging"
      contains: ["logger = logging.getLogger(__name__)", "logger.info"]
    - path: "src/analysis/statistical_analysis.py"
      provides: "Statistical analysis with structured logging"
      contains: ["logger = logging.getLogger(__name__)", "logger.info"]
    - path: "src/analysis/data_overview.py"
      provides: "Data overview with structured logging"
      contains: ["logger = logging.getLogger(__name__)", "logger.info"]
  key_links:
    - from: "src/analysis/*.py"
      to: "src/config/logging_config.py"
      via: "from config.logging_config import setup_logging"
      pattern: "setup_logging"
---

<objective>
Replace all print() diagnostic statements with logger.info() in analysis scripts. Add logging imports and entry point setup.

Purpose: Enables log level control, timestamps, and file output; separates diagnostic output from user output
Output: Analysis scripts with logging module usage, minimal print() for user-facing output only
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-PATTERNS.md
@.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md
</context>

<interfaces>
From src/config/logging_config.py:
```python
def setup_logging(level=logging.INFO, log_file=None):
    """Configure logging for the project."""
    # Creates handlers, configures basicConfig
```

Expected logging pattern per module:
```python
import logging
logger = logging.getLogger(__name__)
# Usage: logger.info(), logger.debug(), logger.warning(), logger.error()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() with logger.info() in analyze_data.py</name>
  <files>src/analysis/analyze_data.py</files>
  <read_first>
    - J:\dataanalyst\analyze_data.py (full file - source for print replacement)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 280-354 for print replacement pattern)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 201-221 for logging usage)
  </read_first>
  <action>In src/analysis/analyze_data.py: (1) Add import logging after pathlib import. (2) Add logger = logging.getLogger(__name__) after imports. (3) Replace all print("=" * 60), print("Phase X: ..."), and similar diagnostic output with logger.info(). (4) Replace print(f"数据维度: ...") and similar info lines with logger.info(f"..."). (5) Replace print(f"\n数据概览: ...") with logger.info. (6) Keep print() ONLY for final user-facing JSON output (if any print statements output results for user to read). (7) At end of file, add if __name__ == '__main__': block with from config.logging_config import setup_logging; setup_logging(level=logging.INFO); call main analysis function. Count print() before and after - target: reduce to < 5 print statements for actual user output.</action>
  <verify>
    <automated>grep -c "logger.info" src/analysis/analyze_data.py && grep -c "print(" src/analysis/analyze_data.py</automated>
    <behavior>analyze_data.py has many logger.info, minimal print (less than 5)</behavior>
  </verify>
  <acceptance_criteria>
    - src/analysis/analyze_data.py contains "import logging"
    - File contains "logger = logging.getLogger(__name__)"
    - File contains many logger.info() calls (grep -c >= 10)
    - File contains "from config.logging_config import setup_logging"
    - File contains if __name__ == '__main__': block with setup_logging() call
    - print() count reduced to < 5 (only user-facing output retained)
  </acceptance_criteria>
  <done>analyze_data.py refactored with logging module replacing diagnostic print statements</done>
</task>

<task type="auto">
  <name>Task 2: Replace print() with logger.info() in behavior_analysis.py and statistical_analysis.py</name>
  <files>src/analysis/behavior_analysis.py, src/analysis/statistical_analysis.py</files>
  <read_first>
    - J:\dataanalyst\behavior_analysis.py (full file)
    - J:\dataanalyst\statistical_analysis.py (full file)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 356-394 for behavior/statistical patterns)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 201-221)
  </read_first>
  <action>Apply same logging integration pattern to behavior_analysis.py and statistical_analysis.py: (1) Add import logging, (2) Add logger = logging.getLogger(__name__), (3) Replace all diagnostic print("="...), print("Phase...", print(f"...") status lines with logger.info(), (4) Add entry point with setup_logging() call. Keep print() only for final user-facing output. Target: each file has < 5 print() statements remaining.</action>
  <verify>
    <automated>grep -c "logger = logging.getLogger" src/analysis/behavior_analysis.py && grep -c "logger = logging.getLogger" src/analysis/statistical_analysis.py && grep -c "print(" src/analysis/behavior_analysis.py | head -1 && grep -c "print(" src/analysis/statistical_analysis.py | head -1</automated>
    <behavior>Both scripts have logger instance, print count reduced</behavior>
  </verify>
  <acceptance_criteria>
    - behavior_analysis.py contains "import logging" and "logger = logging.getLogger(__name__)"
    - statistical_analysis.py contains "import logging" and "logger = logging.getLogger(__name__)"
    - Both contain "from config.logging_config import setup_logging"
    - Both have if __name__ == '__main__': with setup_logging() call
    - Both have logger.info() calls replacing diagnostic output
    - print() count in each file: < 5
  </acceptance_criteria>
  <done>behavior_analysis.py and statistical_analysis.py refactored with logging module</done>
</task>

<task type="auto">
  <name>Task 3: Replace print() with logger.info() in data_overview.py and add entry points</name>
  <files>src/analysis/data_overview.py</files>
  <read_first>
    - J:\dataanalyst\data_overview.py (full file)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md
  </read_first>
  <action>Apply same logging integration pattern to data_overview.py. This script outputs JSON summary - the JSON output itself should use print() for user to see, but status/diagnostic messages should use logger.info(). Add entry point with setup_logging() call.</action>
  <verify>
    <automated>grep -c "logger = logging.getLogger" src/analysis/data_overview.py && grep -c "from config.logging_config import setup_logging" src/analysis/data_overview.py</automated>
    <behavior>data_overview.py has logging setup</behavior>
  </verify>
  <acceptance_criteria>
    - data_overview.py contains "import logging"
    - File contains "logger = logging.getLogger(__name__)"
    - File contains "from config.logging_config import setup_logging"
    - File has if __name__ == '__main__': entry point
    - Diagnostic print() replaced with logger.info()
    - JSON output print() retained for user-facing output
  </acceptance_criteria>
  <done>data_overview.py refactored with logging for diagnostics, print retained for JSON output</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logger → stdout/file | Logs may contain file paths and analysis metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | Log file content | accept | Logs contain file paths but no sensitive data in Phase 1 (no user PII) |
</threat_model>

<verification>
Wave 3 logging integration complete when:
- All 4 analysis scripts in src/analysis/ have logging imports
- All have logger = logging.getLogger(__name__)
- All have setup_logging() entry point
- Diagnostic print() replaced with logger.info()

Run: pytest tests/test_infrastructure.py::test_logging_levels -v
Expected: PASS (after this plan completes)
</verification>

<success_criteria>
- [ ] All 4 analysis scripts import logging
- [ ] All have logger = logging.getLogger(__name__)
- [ ] All have setup_logging() in __main__ block
- [ ] Diagnostic print() replaced with logger.info()
- [ ] print() count per file: < 5 (user output only)
- [ ] test_logging_levels passes
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-05-SUMMARY.md when done
</output>