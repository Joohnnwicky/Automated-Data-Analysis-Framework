---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [pytest.ini, tests/conftest.py, tests/test_infrastructure.py]
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05]
user_setup: []

must_haves:
  truths:
    - "pytest runs successfully from project root"
    - "Test files exist with proper structure"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest configuration"
      contains: "[pytest]"
    - path: "tests/conftest.py"
      provides: "shared fixtures and path setup"
      min_lines: 20
    - path: "tests/test_infrastructure.py"
      provides: "Test scaffolds for all INF requirements"
      contains: ["test_requirements_locked", "test_paths_cross_platform", "test_logging_levels", "test_gitignore_patterns", "test_no_macosx_artifacts"]
  key_links:
    - from: "tests/conftest.py"
      to: "src/"
      via: "sys.path.insert"
      pattern: "sys.path.insert"
---

<objective>
Create test infrastructure scaffold before any implementation. Tests will initially fail (expected) - they serve as specification for subsequent waves.

Purpose: Nyquist validation requires automated tests exist before implementation
Output: pytest.ini, tests/conftest.py, tests/test_infrastructure.py with failing test stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-VALIDATION.md
</context>

<interfaces>
<!-- No existing interfaces - greenfield test infrastructure -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest and create pytest.ini</name>
  <files>pytest.ini</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 269-276 for pytest.ini pattern)
  </read_first>
  <action>Install pytest and pytest-cov via pip. Create pytest.ini at project root with testpaths=tests, python_files=test_*.py, addopts=-v --tb=short. No coverage flags in addopts (coverage runs separately).</action>
  <verify>
    <automated>pip show pytest pytest-cov | grep -E "^Name:|^Version:"</automated>
    <behavior>pytest.ini exists with [pytest] section</behavior>
  </verify>
  <acceptance_criteria>
    - pytest installed (pip show returns version)
    - pytest-cov installed (pip show returns version)
    - pytest.ini contains "[pytest]" section
    - pytest.ini contains "testpaths = tests"
    - pytest.ini contains "python_files = test_*.py"
  </acceptance_criteria>
  <done>pytest and pytest-cov installed, pytest.ini created with standard configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/conftest.py with fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 231-260 for conftest.py pattern)
  </read_first>
  <action>Create tests/conftest.py. Add sys.path.insert to include src/ directory. Define project_root fixture returning Path(__file__).parent.parent. Define data_dir fixture returning project_root / 'data' with mkdir(exist_ok=True). Define output_dir fixture returning project_root / 'output' with mkdir(exist_ok=True). Create tests/ directory if it does not exist.</action>
  <verify>
    <automated>test -f tests/conftest.py && grep -c "project_root" tests/conftest.py</automated>
    <behavior>conftest.py exists with 3 fixtures defined</behavior>
  </verify>
  <acceptance_criteria>
    - tests/ directory exists
    - tests/conftest.py exists
    - tests/conftest.py imports pytest, pathlib.Path, sys
    - tests/conftest.py contains "@pytest.fixture def project_root()"
    - tests/conftest.py contains "@pytest.fixture def data_dir(project_root)"
    - tests/conftest.py contains sys.path.insert for src/
  </acceptance_criteria>
  <done>tests/conftest.py created with project_root, data_dir, output_dir fixtures and src/ path setup</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_infrastructure.py with test scaffolds</name>
  <files>tests/test_infrastructure.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 164-222 for test patterns)
    - .planning/phases/01-infrastructure-foundation/01-VALIDATION.md (lines 39-45 for test ID mapping)
  </read_first>
  <action>Create tests/test_infrastructure.py. Define 5 test functions: test_requirements_locked (INF-01) checking requirements.txt exists and contains == not >=, test_paths_cross_platform (INF-02) checking src/analysis/*.py has no C:\Users paths, test_logging_levels (INF-03) checking src/analysis/*.py imports logging, test_gitignore_patterns (INF-04) checking .gitignore contains .planning and __MACOSX, test_no_macosx_artifacts (INF-05) checking no __MACOSX directories exist. Each test should assert conditions that will initially fail. Use Path for all file checks.</action>
  <verify>
    <automated>pytest tests/test_infrastructure.py -v 2>&1 | grep -E "test_requirements|test_paths|test_logging|test_gitignore|test_macosx"</automated>
    <behavior>pytest discovers all 5 tests (they will fail - expected)</behavior>
  </verify>
  <acceptance_criteria>
    - tests/test_infrastructure.py exists
    - File contains function test_requirements_locked
    - File contains function test_paths_cross_platform
    - File contains function test_logging_levels
    - File contains function test_gitignore_patterns
    - File contains function test_no_macosx_artifacts
    - pytest discovers all 5 tests (run pytest tests/ --collect-only)
  </acceptance_criteria>
  <done>test_infrastructure.py created with 5 test stubs for INF-01 through INF-05. Tests will fail until implementation waves complete (expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → Source code | Tests read source files but cannot modify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip install pytest | mitigate | slopcheck already verified pytest [OK] in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
Wave 0 complete when:
- pytest and pytest-cov installed
- pytest.ini exists with standard config
- tests/conftest.py exists with fixtures
- tests/test_infrastructure.py exists with 5 test stubs
- pytest discovers all tests (pytest tests/ --collect-only succeeds)

Tests will fail - this is expected. Wave 0 provides specification, not passing tests.
</verification>

<success_criteria>
- [ ] pytest installed and verifiable via pip show
- [ ] pytest.ini exists at project root
- [ ] tests/conftest.py exists with project_root, data_dir, output_dir fixtures
- [ ] tests/test_infrastructure.py exists with 5 test function stubs
- [ ] pytest --collect-only finds all 5 tests
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md when done
</output>