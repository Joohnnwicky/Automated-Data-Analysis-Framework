---
phase: 01-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [requirements.txt, .gitignore]
autonomous: true
requirements: [INF-01, INF-04]
user_setup: []

must_haves:
  truths:
    - "User can install dependencies with pip install -r requirements.txt"
    - "pip install -r requirements.txt succeeds with locked versions"
    - ".gitignore excludes .planning/ and test artifacts"
  artifacts:
    - path: "requirements.txt"
      provides: "Locked Python dependencies"
      contains: "=="
      min_lines: 10
    - path: ".gitignore"
      provides: "Git exclude patterns"
      contains: [".planning", ".pytest_cache", "__MACOSX"]
  key_links:
    - from: "requirements.txt"
      to: "Python environment"
      via: "pip install -r"
      pattern: "=="
---

<objective>
Create locked dependency manifest and update .gitignore for planning artifacts and test infrastructure.

Purpose: Enables repeatable environment setup across machines; prevents planning artifacts from being committed
Output: requirements.txt with exact versions, updated .gitignore
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- No interfaces needed - config files only -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements.txt with locked versions</name>
  <files>requirements.txt</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 36-50 for pip freeze pattern)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 25-52 for requirements.txt content)
  </read_first>
  <action>Run pip freeze to capture current installed packages. Write output to requirements.txt at project root. Verify file contains pandas, numpy, scipy, openpyxl, python-pptx, Pillow, pytest, pytest-cov with == locked versions. All dependency lines must use == format (not >= or ~=).</action>
  <verify>
    <automated>grep -c "^pandas==" requirements.txt && grep -c "^numpy==" requirements.txt && grep -c "^pytest==" requirements.txt</automated>
    <behavior>requirements.txt exists with locked versions for core packages</behavior>
  </verify>
  <acceptance_criteria>
    - requirements.txt exists at J:\dataanalyst\requirements.txt
    - File contains "pandas==" line
    - File contains "numpy==" line
    - File contains "scipy==" line
    - File contains "openpyxl==" line
    - File contains "pytest==" line
    - File contains no >= or ~= constraints (only ==)
    - pip install -r requirements.txt would succeed (syntax valid)
  </acceptance_criteria>
  <done>requirements.txt created with all installed packages locked with == versions</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore for planning and test artifacts</name>
  <files>.gitignore</files>
  <read_first>
    - J:\dataanalyst\.gitignore (current content)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (lines 111-161 for gitignore pattern)
  </read_first>
  <action>Read existing .gitignore. Append new section "# GSD planning artifacts (Phase 1)" with entries: .planning/, .pytest_cache/, .coverage, htmlcov/. Also verify existing entries include __MACOSX/ and .DS_Store (should already exist). Do not remove any existing entries - only append. Maintain file formatting consistency.</action>
  <verify>
    <automated>grep -c ".planning" .gitignore && grep -c ".pytest_cache" .gitignore && grep -c "__MACOSX" .gitignore</automated>
    <behavior>.gitignore contains all required patterns</behavior>
  </verify>
  <acceptance_criteria>
    - .gitignore exists (already present)
    - .gitignore contains ".planning" entry (or ".planning/")
    - .gitignore contains ".pytest_cache" entry (or ".pytest_cache/")
    - .gitignore contains ".coverage" entry
    - .gitignore contains "__MACOSX" entry (already present - verify exists)
    - .gitignore contains ".DS_Store" entry (already present - verify exists)
    - Original entries preserved (no deletions)
  </acceptance_criteria>
  <done>.gitignore updated to exclude .planning/, .pytest_cache/, .coverage, htmlcov/ while preserving existing patterns</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip freeze → requirements.txt | Captures actual installed versions |
| Git → .gitignore | Controls which files are tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-SC | Tampering | pip freeze output | accept | pip freeze captures installed versions - no external packages added in this task |
</threat_model>

<verification>
Wave 1 dependency management complete when:
- requirements.txt exists with locked versions
- All core packages (pandas, numpy, scipy, pytest) have == constraints
- .gitignore updated with .planning/, .pytest_cache/, .coverage
- Existing .gitignore entries preserved

Run: pytest tests/test_infrastructure.py::test_requirements_locked -v
Expected: PASS (after this plan completes)
</verification>

<success_criteria>
- [ ] requirements.txt exists at project root
- [ ] requirements.txt contains pandas==, numpy==, scipy==, pytest==
- [ ] No >= or ~= constraints in requirements.txt
- [ ] .gitignore contains .planning entry
- [ ] .gitignore contains .pytest_cache entry
- [ ] .gitignore contains __MACOSX entry
- [ ] test_requirements_locked passes after this plan
</success_criteria>

<output>
Create .planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md when done
</output>